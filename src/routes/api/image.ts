import type { RequestHandler } from './__types/image';

export const POST: RequestHandler = async () => {
	// const data = await request.json();
	// const image = data.image;
	// const imageBorderRadius = data.imageBorderRadius;

	return {};
};
